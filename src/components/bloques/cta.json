{
  "collectionName": "components_bloques_cta",
  "info": {
    "displayName": "Bloque CTA",
    "description": "Llamada a la accion reutilizable",
    "icon": "cursor"
  },
  "options": {},
  "attributes": {
    "titulo": {
      "type": "string",
      "required": true
    },
    "descripcion": {
      "type": "text",
      "required": false
    },
    "textoBoton": {
      "type": "string",
      "required": true,
      "default": "Contactar"
    },
    "urlBoton": {
      "type": "string",
      "required": true
    },
    "variante": {
      "type": "enumeration",
      "enum": ["primario", "secundario", "outline"],
      "default": "primario",
      "required": true
    }
  }
}
